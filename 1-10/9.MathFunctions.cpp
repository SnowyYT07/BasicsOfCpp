#include <iostream>
#include <cmath> // some functions of math like pow(power), sqrt(square root), abs(absolute value)

int main()
{
	double x = 3;
	double y = 4.12;
	double z;
/*

	z = std::max(x, y); // max between two values
	z = std::min(x, y); // min between two values
	z = pow(x, y); // x to the power of y
	z = sqrt(9); // square root of any number
	z = abs(-3); // absolute value of any number
	z = round(y); // round a number (3.6 = 4 || 3.4 = 3)
	z = ceil(y); // round up a number (3.4 = 4)
	z = floor(y); // round down a number (3.8 = 3)

*/
std::cout << z;
}
